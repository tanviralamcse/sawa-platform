describe('Provider Application E2E', () => {
  it('should allow provider to apply to a service request', () => {
    // cy.login('provider');
    // cy.visit('/service-requests');
    // Apply to request, assert application
    // ...
  });
});
