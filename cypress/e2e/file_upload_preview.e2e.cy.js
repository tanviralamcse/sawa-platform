describe('File Upload & Preview E2E', () => {
  it('should allow user to upload a file and preview it', () => {
    // cy.login('buyer');
    // cy.visit('/service-request/new');
    // cy.get('input[type="file"]').attachFile('testfile.pdf');
    // cy.get('.file-preview').should('be.visible');
    // cy.get('.file-preview').should('contain', 'testfile.pdf');
    // ...
  });
});
